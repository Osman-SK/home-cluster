apiVersion: v1
kind: Service
metadata:
  name: couchdb
  namespace: livesync
  labels:
    app: couchdb
spec:
  type: ClusterIP
  selector:
    app: couchdb
  ports:
    - name: http
      port: 5984
      targetPort: http
